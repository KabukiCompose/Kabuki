.gradle/
# The root build only: every module writes into it (layout.buildDirectory in the
# root build script), so one line covers them all.
/build/
# consumer-check is a separate Gradle build and keeps its output to itself.
consumer-check/build/
.idea/
.kotlin/
*.iml
local.properties
out/
captures/
*.hprof

# macOS
.DS_Store
