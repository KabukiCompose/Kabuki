.gradle/
# Any level. Modules write into the root build/ (layout.buildDirectory in the
# root script), but the ignore must not depend on that line staying there.
build/
.idea/
.kotlin/
*.iml
local.properties
out/
captures/
*.hprof

# Left behind by a local Marathon run: the APKs it consumes, its reports and the
# runner itself.
apks/
marathon-report/
marathon.zip
marathon-*/

# macOS
.DS_Store

docslocal/
